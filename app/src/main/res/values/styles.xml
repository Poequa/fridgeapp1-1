<resources>
    <!-- Provide a fallback for the Filled MaterialButton style referenced by older layouts -->
    <style name="Widget.MaterialComponents.Button.Filled" parent="@style/Widget.MaterialComponents.Button" />

    <style name="IngredientCell">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_margin">8dp</item>
        <item name="android:layout_columnWeight">1</item>
        <item name="android:layout_gravity">fill_horizontal</item>
        <item name="android:gravity">center_horizontal</item>
    </style>
</resources>